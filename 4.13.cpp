/*#include<stdio.h>
int main()
{
    int n;
    int num[105];
    int temp[104];
    int i,j;
    int max=99999;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&num[i]);
        if(i>=1)
        {
             if(max>num[i]-num[i-1])
                max=num[i]-num[i-1];
        }
    }
    printf("%d",max);
    return 0;
}
*/

#include<stdio.h>
int main()
{

    return 0;
}
